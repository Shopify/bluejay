# Multiple named operations (valid — no anonymous)
query getDogName {
  dog { name }
}

query getHumanName {
  human { name }
}

# Anonymous operation with fragment (valid when it's the only operation)
