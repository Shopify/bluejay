# Args are known deeply
query argsKnownDeeply {
  dog {
    owner {
      pets {
        ... on Dog {
          doesKnowCommand(dogCommand: SIT)
        }
      }
    }
  }
}

# Ignores args of unknown fields (caught by FieldSelections rule)
# The argument rule should not additionally error on args of fields that don't exist
