# Fails with many more than one root field
subscription manyRootFields {
  newMessage { body }
  a: newMessage { body }
  b: newMessage { body }
}

# Anonymous subscription with multiple root fields
subscription {
  newMessage { body }
  a: newMessage { sender }
}
